import { ModuleMeta } from '../../types/module';

export const title = 'Text Transformer';
export const route = '/text-transformer';
export const description = 'Transform text with case conversions and formatting tools.';
export const icon = '🔤';

const meta: ModuleMeta = {
  title,
  route,
  description,
  icon,
};

export default meta;