import { ModuleMeta } from '../../types/module';

export const title = 'Void Text Box';
export const route = '/void-text-box';
export const icon = '🕳️';

const meta: ModuleMeta = {
  title,
  route,
  icon,
};

export default meta;