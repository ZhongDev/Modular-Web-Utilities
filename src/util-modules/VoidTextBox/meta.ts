import { ModuleMeta } from '../../types/module';

export const title = 'Void Text Box';
export const route = '/void-text-box';
export const description = 'A safe space that sends any message away into the void.';
export const icon = '🕳️';

const meta: ModuleMeta = {  
  title,
  route,
  description,
  icon,
};

export default meta;