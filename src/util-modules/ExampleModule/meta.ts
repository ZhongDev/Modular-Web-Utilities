import { ModuleMeta } from '../../types/module';

export const title = 'Text Transformer';
export const route = '/text-transformer';
export const icon = '🔤';

const meta: ModuleMeta = {
  title,
  route,
  icon,
};

export default meta;