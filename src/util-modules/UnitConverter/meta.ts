import { ModuleMeta } from '../../types/module';

export const title = 'Unit Converter';
export const route = '/unit-converter';
export const description = 'Convert between common units like length, weight, and temperature.';
export const icon = '⚖️';

const meta: ModuleMeta = {
  title,
  route,
  description,
  icon,
};

export default meta;